import { Logo } from "../Logo";
import { StyledHeader } from "./styled";

export const Header = () => {
    return(
        <StyledHeader>
            <Logo />
        </StyledHeader>
    )
}