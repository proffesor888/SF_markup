import { Logo } from "@components/Logo";
import { StyledHeader } from "./styled";

export const Header = () => {
    return(
        <StyledHeader>
            <Logo />
        </StyledHeader>
    )
}